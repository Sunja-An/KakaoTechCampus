CREATE TABLE todo(
    todo_id INTEGER,
    todo_title VARCHAR,
    author INTEGER,
    todo_password VARCHAR
);

CREATE TABLE author(
    author_id INTEGER,
    author_name VARCHAR,
    author_email VARCHAR
)